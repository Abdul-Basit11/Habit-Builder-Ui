import 'package:flutter/material.dart';

import 'layout/body.dart';

class CourseOverviewView extends StatelessWidget {
  const CourseOverviewView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CousreOverviewViewBody();
  }
}
