
import 'package:flutter/material.dart';

import 'layout/body.dart';

class AnalyticView extends StatelessWidget {
  const AnalyticView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AnalyticViewBody();
  }
}
